# A1_OUTPUT.md

**N:** 001 **Hat:** OPERATOR (Clauder) **Status:** EMITTED

Files were written directly to the repo at their locked paths (rather than embedded as fenced blocks in this document) so that the build, drift checks, and acceptance tests could run against the actual filesystem. Manifest below.

## Manifest — files emitted

```
app/layout.tsx
app/page.tsx
app/globals.css
app/api/recommend/route.ts
components/Hero.tsx
components/AssessmentForm.tsx
components/ResultCard.tsx
components/EvidenceTier.tsx
components/Verdict.tsx
components/HowItWorks.tsx
components/Differentiators.tsx
components/Footer.tsx
lib/engine.ts
lib/supplements.ts
lib/nutrition.ts
lib/types.ts
lib/verdict.ts
tailwind.config.ts
next.config.js
tsconfig.json
package.json
README.md
.env.example
```

23 paths total, matching the locked file tree from `S1_LOCKED.md`.

## Auto-generated artifacts (not in locked tree)

These are produced by tooling, not authored by the Operator:

- `next-env.d.ts` — generated by `next build`
- `package-lock.json` — generated by `npm install`

They are not deliberate scope additions; the Watcher report records them under "tool-generated, not operator drift."

## Engine notes

- `lib/engine.ts` exports `recommend(input: UserInput): Recommendation` as a pure synchronous function. No `fetch`, no `setTimeout`, no async, no randomness.
- 14 supplement entries fully populated in `lib/supplements.ts` with dose, timing, evidence tier, RCT-count band, goal/symptom mapping, and contraindications. Real-format PMIDs are attached only where the Operator was confident they map to well-known human-trial work; remaining `pubmedExample` fields are intentionally `undefined` rather than fabricated.
- Color palette restricted to ink (`#0A0A0A`), paper (`#FAFAF7`), lime (`#D4FF3A`), clinical (`#FF6B35`). All component classes draw from this palette only.
- `next/font/google` provides Fraunces (serif headlines) and JetBrains Mono (data/labels). Body uses system sans. Three families exactly.
- The home page (`app/page.tsx`) is a server component; `Hero` / `AssessmentForm` are the only `"use client"` boundaries.
