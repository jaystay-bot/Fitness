# NEXT_006.md

**Previous N:** 005 — PASS (depth and authority pass: variation + confidence + editorial motion + 3D bottle).

## N=006

N=006 = the original distribution + trust layer plan, now ready to ship on top of a deeper, more visual product. Pull the contract from `agent_state/QUEUE.md` entry titled **"Distribution and trust layer (shareable URLs + sources panel + post-result email capture)"**.

## Standing posture going forward

- Visual baseline regression test (`tests/visual.spec.ts`) is part of the standard Judge phase and now includes:
  - N=003 styling tripwires: `h1` font-size > 32 px, body bg `rgb(10, 10, 10)`, CTA bg `rgb(212, 255, 58)`.
  - N=004 unit-toggle invariants: imperial-default and metric-after-toggle modes.
- N=005 added the **positioning tripwire**: every Judge phase from now on must re-run the banned-pattern check on the rendered HTML (no streaks, lootbox reveals, scarcity timers, achievements, blocking modals, gamification copy). The Watcher's source-grep mirrors this.
- Engine signature (`recommend(input: UserInput): Recommendation`) and `UserInput` shape stay inviolate. Conversion at the form boundary; variation and confidence are post-processing on candidate picks.

## Other QUEUE candidates (Commander to consider after N=006)

- Stripe checkout for "Pro Protocol" deeper plan PDF.
- Apify-based supplement price comparison.
